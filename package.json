{
  "name": "tabito-lib",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild *.ts --outdir=. --platform=node --format=cjs",
    "demo": "npm run build && node demo.js",
    "test": "npm run build && esbuild tests/*.ts --outdir=tests --platform=node --format=cjs && tape 'tests/**/*js'"
  },
  "keywords": [],
  "author": "",
  "license": "Unlicense",
  "devDependencies": {
    "@types/node": "^20.8.10",
    "@types/tape": "^5.6.3",
    "esbuild": "^0.19.5",
    "tape": "^5.7.2",
    "typescript": "^5.2.2"
  }
}
